import { ActivityLogService } from '../activity-log/activity-log.service';
import { ContactHistoryService } from '../contacts/contact-history.service';
import { ContactsService } from '../contacts/contacts.service';
import { FinanceService } from '../finance/finance.service';
import { FinanceToolsService } from '../finance/finance-tools.service';
import { MemoryService } from '../memory/memory.service';
import { MeetingsService } from '../meetings/meetings.service';
import { NotesService } from '../notes/notes.service';
import { RemindersService } from '../reminders/reminders.service';
import { TasksService } from '../tasks/tasks.service';
import { TodayService } from '../today/today.service';
import { TelegramIntegrationService } from '../telegram/telegram-integration.service';
import { GoogleCalendarService } from '../google/google-calendar.service';
import { GoogleDriveService } from '../google/google-drive.service';
import { FilesService } from '../files/files.service';
import { AIToolDefinition, AIToolMetadata } from './types/ai-tool.types';
export declare class AIToolRegistryService {
    private readonly tasksService;
    private readonly remindersService;
    private readonly meetingsService;
    private readonly notesService;
    private readonly contactsService;
    private readonly contactHistoryService;
    private readonly memoryService;
    private readonly financeService;
    private readonly financeToolsService;
    private readonly todayService;
    private readonly telegramIntegrationService;
    private readonly activityLog;
    private readonly googleCalendarService?;
    private readonly googleDriveService?;
    private readonly filesService?;
    private readonly tools;
    constructor(tasksService: TasksService, remindersService: RemindersService, meetingsService: MeetingsService, notesService: NotesService, contactsService: ContactsService, contactHistoryService: ContactHistoryService, memoryService: MemoryService, financeService: FinanceService, financeToolsService: FinanceToolsService, todayService: TodayService, telegramIntegrationService: TelegramIntegrationService, activityLog: ActivityLogService, googleCalendarService?: GoogleCalendarService | undefined, googleDriveService?: GoogleDriveService | undefined, filesService?: FilesService | undefined);
    listMetadata(): Array<Pick<AIToolMetadata, 'name' | 'category' | 'requiresConfirmation' | 'sideEffect'>>;
    getToolDefinitionsForModel(): AIToolMetadata[];
    get(name: string): AIToolDefinition<unknown, unknown>;
    private register;
    private base;
    private registerTools;
    private assertContactOwned;
    recordWriteExecution(toolName: string, userId: string, result: unknown): Promise<void>;
    private entityTypeFor;
}
